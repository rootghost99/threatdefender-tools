{
  "routes": [
    // Allow login/logout pages
    { "route": "/.auth/*", "allowedRoles": ["anonymous"] },

    // Allow your API to be called publicly (e.g., from Postman)
    { "route": "/api/ThreatIntelLookup", "allowedRoles": ["anonymous"] },

    // Default: everything else requires login
    { "route": "/*", "allowedRoles": ["authenticated"] }
  ]
}
